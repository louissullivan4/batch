# pnpm: keep the store strict and deterministic for a solo-maintained repo.
auto-install-peers=true
strict-peer-dependencies=false
# Internal @batch/* packages are published-by-source (exports point at src/*.ts);
# no hoisting surprises for workspace deps.
link-workspace-packages=true
